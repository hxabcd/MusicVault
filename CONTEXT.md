# MusicVault 领域上下文

MusicVault 管理曲目及其媒体资产，并将这些资产单向同步到一个或多个用户定义的目标端。

本文中的项目术语采用“中文（English）”形式；代码、API 和外部 Preset 文档优先使用括号中的英文术语。

## 语言

**曲目（Track）**：网易云音乐中的一首可被 MusicVault 管理的音乐作品。
_避免_：歌曲文件、音频文件（这些指媒体资产，而不是曲目本身）

**歌单（Playlist）**：曲目的有序或有归属关系的集合，可作为目标同步的筛选依据。
_避免_：目录（目录是目标端的表现形式，不等于歌单）

**媒体资产（Media Asset）**：属于某个曲目的可持久化内容，例如音频、歌词或封面。
_避免_：导出文件（导出文件是目标端的结果，媒体资产是源侧内容）

**预设（Preset）**：由用户定义的一套目标同步规则，描述选择哪些曲目、使用哪些媒体表现以及如何写入目标端。
_避免_：配置项、输出目录（Preset 的范围大于单个配置项或目录）

**目标端（Target）**：接收 MusicVault 单向同步结果的本地目录、设备、播放器或外部系统。
_避免_：library（library 只是其中一种目标端表现）

**目标同步器（Target Synchronizer）**：实现某个 Preset 的目标策略，负责把源侧曲目和媒体资产转换为目标端操作。
_避免_：导出器（导出器只表达单向生成，不能覆盖后续目标同步场景）

**源快照（Source Snapshot）**：一次同步开始时确定的、供所有 Preset 共享的源侧不可变视图。
_避免_：当前状态（当前状态可能在运行中变化，源快照强调一次运行内的一致性）

**单向同步（One-way Synchronization）**：数据只从 MusicVault 源侧流向目标端，目标端的修改不会反向写入源侧。
_避免_：双向同步、镜像（镜像是某种删除策略，不等同于同步方向）
