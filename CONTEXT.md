# MusicVault 领域上下文

MusicVault 管理曲目及其媒体资产，并将这些资产单向同步到一个或多个用户定义的目标端。

本文中的项目术语采用“中文（English）”形式；代码、API 和外部 Preset 文档优先使用括号中的英文术语。

## 语言

**曲目（Track）**：网易云音乐中的一首可被 MusicVault 管理的音乐作品。
_避免_：歌曲文件、音频文件（这些指媒体资产，而不是曲目本身）

**歌单（Playlist）**：曲目的有序或有归属关系的集合，可作为目标同步的筛选依据。
_避免_：目录（目录是目标端的表现形式，不等于歌单）

**媒体资产（Media Asset）**：属于某个曲目的可持久化内容，例如音频、歌词或封面。
_避免_：导出文件（导出文件是目标端的结果，媒体资产是源侧内容）

**预设（Preset）**：以 Python 脚本声明的处理规格，描述音频规格（音质/格式/码率）、歌词输出函数与元数据粒度；sync_target 脚本按名称引用预设并定义目标端写入逻辑。
_避免_：配置项、输出目录（Preset 的范围大于单个配置项或目录；声明式 presets 配置数组已退役，改由脚本声明）

**目标端（Target）**：接收 MusicVault 单向同步结果的本地目录、设备、播放器或外部系统。
_避免_：library（library 只是其中一种目标端表现）

**目标同步器（Target Synchronizer）**：实现某个 Preset 的目标策略，负责把源侧曲目和媒体资产转换为目标端操作。
_避免_：导出器（导出器只表达单向生成，不能覆盖后续目标同步场景）

**源快照（Source Snapshot）**：一次同步开始时确定的、供所有 Preset 共享的源侧不可变视图。
_避免_：当前状态（当前状态可能在运行中变化，源快照强调一次运行内的一致性）

**统一歌词格式（Unified Lyrics Format）**：按行结构化的歌词中间表示（LyricLine/LyricWord，含逐字、翻译、罗马音），由 pull 阶段从源端原始歌词转换后存入 SQLite，process 阶段离线消费；preset 的 `build_lyrics` 函数逐行渲染为目标文本（标准 LRC、增强歌词等），由框架按行拼接成歌词文件。
_避免_：文本级合并（文本级翻译合并已退役，渲染基于行模型）

**单向同步（One-way Synchronization）**：数据只从 MusicVault 源侧流向目标端，目标端的修改不会反向写入源侧。
_避免_：双向同步、镜像（镜像是某种删除策略，不等同于同步方向）

## 流水线阶段

**拉取（fetch）**：sync 第一阶段，拉取歌单元数据并更新 SQLite 关系（改名/删除/分配变化），不下载、不碰 library。
_避免_：同步（同步是整条流水线，fetch 只是元数据阶段）

**下载（pull）**：sync 第二阶段，对比并下载新增曲目、获取详情与歌词并转统一歌词格式入库、清理远端已删除曲目。
_避免_：下载（下载只是其中一部分，pull 还负责歌词入库与清理）

**处理（process）**：sync 第三阶段，对本地下载文件按 preset 声明离线执行解码、转码、写元数据与歌词文件。
_避免_：后处理（同义，术语统一用处理）

**分发（distribute）**：sync 第四阶段（或独立命令），从 SQLite 源快照按 sync_target 声明重建目标端（内置 hardlink 重建 library/）。
_避免_：目标同步、导出（分发强调从源快照重建目标端）
